<div class="container">

  <div class="row">

    <div class="col-6">
      <div class="container min-vh-100">
    <div style="margin: 130px 0px 40px 0px">
      <h1>My vans</h1>
    </div>
    <div class="row">
      <% @my_vans.each do |van| %>
        <div class="col-6">
          <div>
            <%= link_to van_path(van) do %>
            <%= render "shared/card", van: van %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
    </div>

    <div class="col-6">
      <div class="container min-vh-100">
    <div style="margin: 130px 0px 40px 0px">
      <h1>My Bookings</h1>
    </div>
    <div class="row">
      <% @my_bookings.each do |van| %>
        <div class="col-6">
          <div>
            <%= link_to van_path(van) do %>
              <%= render "shared/card", van: van %>
               <% end %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>

  </div>

</div>
